spring:
  application:
    name: hystrix-dashboard

server:
  port: 1301

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, info, health
#      base-path: /
#          include: hystrix.stream, info, health